# Gap Foundry Web

AI 기반 시장 검증 서비스의 웹 프론트엔드입니다.

## 🚀 시작하기

### 1. 백엔드 서버 실행

먼저 Gap Foundry API 서버를 실행해야 합니다:

```bash
# 프로젝트 루트에서
cd /Users/hoonchan/Desktop/Utopify/gap_foundry
source .venv/bin/activate  # 가상환경 활성화 (있는 경우)
uvicorn gap_foundry.api:app --reload --host 0.0.0.0 --port 8000
```

### 2. 프론트엔드 개발 서버 실행

```bash
cd web
npm run dev
```

브라우저에서 http://localhost:3000 으로 접속합니다.

## 📁 프로젝트 구조

```
web/
├── src/
│   ├── app/
│   │   ├── page.tsx              # 메인 페이지 (아이디어 입력 폼)
│   │   ├── validate/
│   │   │   └── [runId]/
│   │   │       └── page.tsx      # 검증 결과 페이지
│   │   ├── layout.tsx
│   │   └── globals.css
│   ├── components/
│   │   ├── ui/                   # Shadcn UI 컴포넌트
│   │   └── idea-form.tsx         # 아이디어 입력 폼
│   └── lib/
│       ├── api.ts                # API 클라이언트
│       └── utils.ts
├── .env.local                    # 환경 변수
└── package.json
```

## 🔧 환경 변수

`.env.local` 파일:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## ✨ 주요 기능

### 1. 실시간 PreGate 체크
- 입력하는 동안 실시간으로 아이디어의 구체성을 검사
- LLM 호출 없이 빠른 피드백 제공
- 개선 제안 표시

### 2. 비동기 검증 실행
- 검증 요청 후 백그라운드에서 처리
- SSE(Server-Sent Events)로 실시간 진행 상태 업데이트
- 페이지를 떠나도 검증은 계속됨

### 3. 결과 대시보드
- GO/HOLD/NO 판정 결과 표시
- 전체 리포트 마크다운 렌더링
- 리포트 다운로드 지원

## 🎨 기술 스택

- **Framework**: Next.js 15 (App Router)
- **Styling**: Tailwind CSS v4
- **UI Components**: Shadcn UI
- **State Management**: React useState/useEffect
- **API Communication**: Fetch + SSE

## 📝 API 엔드포인트

| 메서드 | 경로 | 설명 |
|--------|------|------|
| POST | `/pregate` | PreGate 빠른 체크 (동기) |
| POST | `/validate` | 검증 시작 (비동기) |
| GET | `/status/{runId}` | 작업 상태 조회 |
| GET | `/stream/{runId}` | SSE 실시간 스트림 |
| GET | `/report/{runId}` | 리포트 조회 |
| GET | `/report/{runId}/download` | 리포트 다운로드 |
| GET | `/jobs` | 최근 작업 목록 |
